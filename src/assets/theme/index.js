import { createTheme } from "@mui/material/styles";
import { palette } from "./base/palette";
import { typography } from "./base/typography";

const wolibaTheme = createTheme({
  palette,
  typography,
});

export default wolibaTheme;
